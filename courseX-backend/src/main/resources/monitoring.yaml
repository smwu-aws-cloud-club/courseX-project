management:
  health:
    redis:
      enabled: false
  endpoints:
    jmx:
      exposure:
        exclude: "*"
    web:
      exposure:
        include: health, info, metrics, prometheus
      base-path: ${ACTUATOR_PATH}